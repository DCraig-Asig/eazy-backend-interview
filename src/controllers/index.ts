import { AppController } from './app.controller';
import { ExchangeRateController } from './exchange-rate.controller';

export const Controllers = [AppController, ExchangeRateController];

export { AppController, ExchangeRateController };
